# ai-coding-rules-scaffold — session handoff (auto-generated)

**Generated:** 2026-07-19 16:50 UTC

Auto-written by `~/.claude/bin/write_handoff.sh` (called from the
`/handoff` skill + the `SessionEnd` hook in `~/.claude/settings.json`).
Auto-loaded into the next session by the `SessionStart` hook in the
same settings file. Always lives at `<repo>/.claude/handoff_current.md`;
the previous handoff is rotated to `.claude/handoff_history/` before
overwrite (last 5 retained; override via `HANDOFF_HISTORY_KEEP`).
Run `/handoff-more` in a fresh session to pull older handoffs into context.

---

## Repo: ai-coding-rules-scaffold

**HEAD:** `7bae321` — Merge pull request #61 from Sting25/chore/homebrew-v0.11.0

**Branch:** `main` (main...origin/main)

### Recent commits

```
7bae321 Merge pull request #61 from Sting25/chore/homebrew-v0.11.0
29958df chore(homebrew): bump canonical formula to v0.11.0
25a32f6 chore: gitignore .claude/handoff_backups/
15e8e8b Merge pull request #60 from Sting25/release/v0.11.0
17a13b2 release: v0.11.0
3ccca2b Merge pull request #59 from Sting25/fix/actionlint-macos-arch
5c02aa2 ci: make the actionlint install OS/arch-aware (fix macOS runner)
fd37779 Merge pull request #58 from Sting25/fix/code-security-review-2026-07
cf21b9b style: satisfy shellcheck SC1007 in _mkdir_safe (explicit empty init)
bf69f0c test: cover eslint + php -l pre-commit linter blocks (disputed-finding residual)
```

### Working tree

```
 M .gitignore
?? .claude/
```

## Verify state matches reality

```bash
git -C /Users/brain/ai-coding-rules-scaffold status && git -C /Users/brain/ai-coding-rules-scaffold log --oneline -5
```

<!-- HANDOFF_BIND_BEGIN -->
## Rules (fences — carried into the next session)

<!-- HANDOFF_RULES_PLACEHOLDER: /handoff may replace this comment with explicit scope fences. Only content inside the BIND markers loads as binding (and only when provenance verifies); leave this comment in place for none. -->
<!-- HANDOFF_BIND_END -->

---

## Notes from this session

**Clean boundary — nothing in-flight.** This session shipped a full
code+security review and cut **v0.11.0**. All major work is merged,
released, and verified; there is no track awaiting pickup.

### What happened (all DONE, in git/npm/brew)
- **RPM packaging deferred** — deleted the untracked `packaging/rpm/`
  draft; full spec+README preserved in **GitHub issue #57**. Do NOT
  re-propose RPM without real user demand.
- **Code+security review** (multi-agent workflow) → 30 confirmed
  findings fixed → **PR #58**. Plus a new bug found mid-review: a
  macOS/BSD awk NUL-truncation bypass across all scanners.
- **PR #59** fixed a regression #58 introduced (hardcoded `linux_amd64`
  actionlint asset broke the macOS CI runner) → arch-aware install.
- **v0.11.0 released** (PR #60 + tag): npm publish (OIDC) + GitHub
  Release both succeeded; **PR #61** + tap self-bump put Homebrew at
  0.11.0 too. Verified: all four channels agree — check with
  `npm view ai-coding-rules-scaffold version` (→ 0.11.0),
  `gh release view v0.11.0`, and the `url` tag in both
  `packaging/homebrew/*.rb` and the `Sting25/homebrew-tap` formula.
- **Install smoke-tested** end-to-end on both npm (`npx …@0.11.0`) and
  Homebrew (`brew install/test`, then uninstalled) — installed hook
  rejects a NUL-after-`ABIA` secret in both. Memory written:
  `scaffold-2026-07-19-full-review.md`.

### Uncommitted (decide, don't reflexively commit)
- Working tree shows `M .gitignore` + `?? .claude/`. The `.gitignore`
  edits are handoff-tooling bootstrap (the `write_handoff.sh` run just
  added `.claude/handoff_current.md`, `handoff_history/`,
  `handoff_pinned.md`); `.claude/` is local handoff state. These are
  NOT project changes — commit the `.gitignore` additions only if you
  want them tracked, otherwise leave them.

### Lessons worth carrying (not yet codified anywhere)
- **Releases: wait for CI green on BOTH runners before merging.** I
  merged #58 without waiting; it went red on macos-latest (the
  actionlint arch bug) and needed the #59 follow-up. The repo has no
  branch protection, so nothing enforces this — it's on you.
- The scaffold's **500-line module cap is dogfooded/real** — its own
  pre-commit blocked an over-cap test file this session; budget for it
  (split a case file or extract a module, never suppress).

### Optional follow-ups (offered, NOT acted on)
- `prettier`/`phpcs` pre-commit linter blocks are still lightly covered
  (noted in the bf69f0c commit body).
- Handoff-project enhancement idea (surfaced to user, they may pass to
  the handoff maintainer): the installer's hook-presence check is
  substring-only, so an argument change to a hook needs a hand-written
  `migrate_legacy_*` function or the old wiring persists silently. A
  self-healing "reconcile to canonical command when marker matches but
  full command differs" would remove that fragility. Confirmed the
  current install is correct (hooks byte-match v0.10.0), so this is an
  enhancement, not a bug.

### Environment note
- Latest handoff (v0.10.0) is installed globally in `~/.claude`; skills
  + bin are symlinked into `/Users/brain/dev/handoff`, whose remote was
  switched from SSH → HTTPS this session (SSH auth was failing). Update
  via `git -C /Users/brain/dev/handoff pull`; no reinstall needed unless
  hooks/permissions change.
<!-- HANDOFF_HMAC: b840a0921002b92c7aea1c9229ede2b6f6a1075c501c6f3e2aded79b8eb35d47 -->
