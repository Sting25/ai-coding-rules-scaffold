# CLAUDE.md

See [AGENTS.md](./AGENTS.md) — this project uses the cross-tool `AGENTS.md` convention. Claude Code auto-loads this file on start; the references below pull `AGENTS.md` and `coding-rules.md` into context. (`AGENTS.md` links `coding-rules.md` rather than importing it, because `@` imports are Claude Code syntax and `AGENTS.md` stays cross-tool; the import lives here so the rules are actually in context at session start instead of a link the model may or may not follow.)

@AGENTS.md
@coding-rules.md
